#!/usr/bin/python

class SubsetGenerator:
    def __init__(self, fname=None):
        self.fname = fname

    def __readline(self, line):
        dim_data = split(' ')
        max_dim = max(map(lambda x : x.split(':')[0], dim_data))

    def __readsparsedata__(self):
        
